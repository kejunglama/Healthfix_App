import 'package:flutter/material.dart';

import 'components/body.dart';

class FitnessTrainersScreen extends StatelessWidget {
  const FitnessTrainersScreen();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Body(),
    );
  }
}
